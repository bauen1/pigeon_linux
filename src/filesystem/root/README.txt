Stuff you should know:
* root doesn't have a password by default
* keymaps are under /usr/share/kbd/keymaps
* load a keymap using 'loadkeys <keymap name>' for example:
    % loadkeys us
    % loadkeys de
